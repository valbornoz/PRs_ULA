#ifndef CONJUNTOS_H
#define CONJUNTOS_H

class Conjuntos
{
    public:
        Conjuntos() {}
        virtual ~Conjuntos() {}
       // virtual T& search(const T ele)=0;
       // virtual void insert(T ele)=0;
       // virtual T& remove(const T ele)=0;
     //   virtual void merge(Conjuntos& t1, Conjuntos& t2)=0;
      //  virtual void split(Conjuntos& t1, Conjuntos& t2)=0;
      //  virtual T& searchInPos(const int pos)=0;
      //  bool isEmpty()=0;


};

#endif // CONJUNTOS_H
